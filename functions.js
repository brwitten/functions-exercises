// TODO: add your solutions here!

// 1. Build your own concatenation

function combineWords(word1,word2) {
  console.log(x+y);
}

var word1 = "jumping"
var word2 = "elephants"
combineWords(x,y)

// 2. Repeat a phrase

function parrot(phrase, n) {
  while (n > 0) {
      console.log(phrase);
      n = n-1;
      parrot();
    }
}

parrot("hello!",3)

// 3. Build your own Power function

function power(base,exponent) {
  return base ** exponent
}

var result = power(2,2);
console.log(result);
